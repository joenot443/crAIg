--Evolve crAIg into the next generation
function newGeneration()
	-- 1. Survival of the Fittest
	for s=1,#crAIg.species do
		survivalOfTheFittest(crAIg.species[s])
	end

	-- 2. Remove any species that have been around for too long without being good

	-- 3. Remove any species that are so weak they're without hope

	-- 4. Make babies!

	-- 5. Re-organize species accordingly

	-- 6. Done!
end